/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://ezbooq.com", // Change this to your actual domain
  generateRobotsTxt: true, // Generate a robots.txt file
  generateIndexSitemap: false, // Optional: Set to true if needed
};
